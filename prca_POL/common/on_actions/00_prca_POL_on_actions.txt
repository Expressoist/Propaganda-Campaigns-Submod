# ADDITIONAL INFORMATION:
# Use "set_country_flag = prca_disable_default_decisions" if you 
# want to overwrite the posters of countries which are already 
# included in the mod (e.g. Germany)

on_actions = {
	on_startup = {
		effect = {
			POL = {
				set_country_flag = prca_v0.8.1
				set_country_flag = prca_enabled
				set_country_flag = prca_leaflets_enabled
			}
		}
	}
}